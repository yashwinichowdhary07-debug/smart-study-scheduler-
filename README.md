# smart-study-scheduler-
Personalized study scheduling web app — React + Flask + simple ML
# create project folder
mkdir smart-study-scheduler
cd smart-study-scheduler

# initialize git and connect to GitHub (replace with your repo URL)
git init
git remote add origin https://github.com/<your-username>/smart-study-scheduler.git

# create folders and an initial README
mkdir frontend backend docs
touch README.md

# commit and push
git add .
git commit -m "chore: initial repo structure"
git branch -M main
git push -u origin main
